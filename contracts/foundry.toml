[profile.default]
src = "src"
test = "test"
out = "../.tare/contracts/out"
cache_path = "../.tare/contracts/cache"
solc_version = "0.8.30"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
fs_permissions = [{ access = "read", path = "./test/fixtures" }]

[profile.default.fuzz]
runs = 256
